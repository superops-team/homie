[package]
name = "ztracing"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"

[features]
default = []
tracy = []

[dependencies]
ztracing_macro = { path = "../ztracing_macro" }
