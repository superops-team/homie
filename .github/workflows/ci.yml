name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  engine:
    name: Swift engine (daemon + CLI)
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v5.0.0
      - name: Validate shell scripts
        run: bash -n scripts/*.sh homie/scripts/*.sh
      - name: Verify agent manifest mirror
        run: scripts/check-agent-manifest-drift.sh
      - name: Verify reviewed regressions stay fixed
        run: homie/scripts/verify-remote-refactor.sh
      - name: Test release publishing guards
        run: |
          bash homie/scripts/test-publish-github-release.sh
          bash homie/scripts/test-publish-homebrew-cask.sh
      - name: Install and audit browser sidecar
        working-directory: sidecar
        run: |
          npm ci
          npm audit --omit=dev
      # --no-parallel is load-bearing on CI. Run in parallel, every test starts
      # at once, and the suite then stalls indefinitely on a 3-core runner: the
      # PTY and holder tests block inside async contexts, and with that few
      # cores the cooperative pool has no thread left to make progress on.
      # A developer Mac has enough cores to hide it. Serial costs ~30s.
      #
      # SwiftPM also block-buffers when stdout is a pipe, so a test process that
      # dies takes most of its console output with it — hence the xunit file.
      # `script` gives the run a pty, which makes SwiftPM line-buffer. Piped, it
      # block-buffers, and a test process that dies takes the tail of its output
      # with it — so the log's last line is wherever the buffer happened to
      # flush, not where the run actually stopped. That cost real debugging
      # time. The xunit file is no help either: it is only written at the end.
      - name: Test
        env:
          # Runner spawns and socket binds are far slower than on a dev Mac; the
          # 5s liveness waits time out there. See waitUntil in the test helpers.
          HOMIE_TEST_TIMEOUT_SCALE: "6"
        run: |
          set -o pipefail
          script -q /dev/null swift test --no-parallel | tee "${RUNNER_TEMP}/test.log"
          grep -q "Test run with .* passed" "${RUNNER_TEMP}/test.log"
      - name: Where it stopped
        if: failure()
        run: tail -40 "${RUNNER_TEMP}/test.log" || true
      - name: Release build
        run: swift build -c release

  app:
    name: Rust app (homie)
    runs-on: macos-15
    timeout-minutes: 60
    defaults:
      run:
        working-directory: homie
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v5.0.0
      # rust-toolchain.toml pins 1.95.0 with clippy + rustfmt.
      - name: Cache cargo registry and target
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            homie/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('homie/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-
      - name: Check dependency license policy
        run: python3 ../scripts/check-licenses.py
      - name: Format
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Test
        run: cargo test --workspace
      - name: Remote Holder acceptance and latency gates
        run: scripts/remote-acceptance.sh
      - name: Release build
        run: cargo build --workspace --release

  remote-helper-native:
    name: Remote Helper (${{ matrix.target }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            target: x86_64-unknown-linux-musl
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-musl
          - runner: macos-15
            target: aarch64-apple-darwin
    defaults:
      run:
        working-directory: homie
    steps:
      - uses: actions/checkout@v7
      - name: Install target
        run: rustup target add "${{ matrix.target }}"
      - name: Install native musl linker
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install --yes musl-tools
      - name: Build exact release Helper
        env:
          HOMIE_REMOTE_BUILD_ID: ci-${{ matrix.target }}
        run: cargo build --locked --release --package homie-remote --target "${{ matrix.target }}"
      - name: Execute native probe and verify identity
        env:
          EXPECTED_TARGET: ${{ matrix.target }}
          EXPECTED_BUILD: ci-${{ matrix.target }}
        run: |
          helper="target/${EXPECTED_TARGET}/release/homie-remote"
          test -x "${helper}"
          probe="$("${helper}" probe --format=json)"
          jq -e \
            --arg target "${EXPECTED_TARGET}" \
            --arg build "${EXPECTED_BUILD}" \
            '.target == $target and .buildId == $build and .protocol.major == 1 and .supported and .holderAvailable' \
            <<<"${probe}"

  remote-ssh-soak:
    name: Remote OpenSSH detach/reconnect
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    defaults:
      run:
        working-directory: homie
    steps:
      - uses: actions/checkout@v7
      - name: Start disposable ordinary-user SSH endpoint
        run: |
          sudo apt-get update
          sudo apt-get install --yes openssh-server
          # sshd refuses to start without its privilege separation directory,
          # which the package does not create on a fresh runner image.
          sudo mkdir -p /run/sshd
          # The runner account ships with no password, which leaves it locked
          # ("!" in shadow) and makes sshd reject it before it ever considers
          # the key. Unlock it without setting one; password and
          # keyboard-interactive auth stay disabled below, so keys remain the
          # only way in on this throwaway host.
          sudo usermod -p '*' "${USER}"
          mkdir -p "${HOME}/.ssh" "${RUNNER_TEMP}/sshd"
          chmod 700 "${HOME}/.ssh"
          ssh-keygen -q -t ed25519 -N '' -f "${RUNNER_TEMP}/homie_ci_key"
          cat "${RUNNER_TEMP}/homie_ci_key.pub" >> "${HOME}/.ssh/authorized_keys"
          chmod 600 "${HOME}/.ssh/authorized_keys"
          printf '%s\n' \
            'Host homie-ci-soak' \
            '  HostName 127.0.0.1' \
            '  Port 2222' \
            "  User ${USER}" \
            "  IdentityFile ${RUNNER_TEMP}/homie_ci_key" \
            '  IdentitiesOnly yes' \
            '  StrictHostKeyChecking accept-new' \
            "  UserKnownHostsFile ${RUNNER_TEMP}/known_hosts" \
            > "${HOME}/.ssh/config"
          chmod 600 "${HOME}/.ssh/config"
          sudo /usr/sbin/sshd -D -e -p 2222 \
            -o PasswordAuthentication=no \
            -o KbdInteractiveAuthentication=no \
            -o UsePAM=no \
            -o AllowUsers="${USER}" \
            -o PidFile="${RUNNER_TEMP}/sshd/sshd.pid" \
            >"${RUNNER_TEMP}/sshd.log" 2>&1 &
          for attempt in $(seq 1 50); do
            if ssh -o BatchMode=yes homie-ci-soak true; then
              exit 0
            fi
            sleep 0.1
          done
          cat "${RUNNER_TEMP}/sshd.log"
          exit 1
      - name: Run real SSH persistence soak
        env:
          HOMIE_REMOTE_SSH_TARGET: homie-ci-soak
          HOMIE_REMOTE_SOAK_SECONDS: "5"
        run: scripts/remote-ssh-soak.sh

  bundle:
    name: Package homie.app (unsigned)
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v5.0.0
      - name: Cache cargo registry and target
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            homie/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('homie/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-
      # package.sh lipos two slices into a universal binary, so both targets
      # have to be installed — and installed for the toolchain homie/ pins in
      # rust-toolchain.toml, not the runner default. Adding them from the repo
      # root silently equips the wrong toolchain and the x86_64 pass then fails
      # with "can't find crate for std".
      - name: Install universal targets
        working-directory: homie
        run: rustup target add aarch64-apple-darwin x86_64-apple-darwin aarch64-unknown-linux-musl x86_64-unknown-linux-musl
      - name: Install Rust packaging tools
        run: |
          brew install zig
          cargo install cargo-packager --locked
          cargo install cargo-zigbuild --locked
      - name: Package
        env:
          HOMIE_DIST_DIR: ${{ runner.temp }}/homie-dist
        run: homie/scripts/package.sh
      - name: Verify bundle structure and signature
        run: |
          app="${RUNNER_TEMP}/homie-dist/homie.app"
          homie/scripts/package.sh --phase verify --app "${app}"
